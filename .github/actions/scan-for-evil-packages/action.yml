name: "Scan for Evil Packages"
description: "Scans a repository for compromised packages across all branches."
inputs:
  slack-webhook-url:
    description: "Slack Webhook URL for notifications"
    required: true
outputs:
  summary:
    description: "Human-readable scan summary for Slack or logs"
    value: ${{ steps.prepare-summary.outputs.summary }}
runs:
  using: "composite"
  steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 24

    - name: Clone hof-vulnerabilities-scanner
      run: git clone https://github.com/UKHomeOfficeForms/hof-vulnerabilities-scanner.git
      shell: bash

    - name: Run vulnerability scan
      run: |
        set -euo pipefail
        echo "\n============================================"
        echo "🔎 Starting Evil Package Scan"
        echo "📦 Repository: $GITHUB_REPOSITORY"
        echo "============================================\n"

        git fetch --all --prune
        branches=$(git ls-remote --heads origin | awk '{print $2}' | sed 's|refs/heads/||')
        summary=""
        any_compromised=0
        repo_name=$(basename "$GITHUB_REPOSITORY")

        echo "::group::🌿 Branch Scans"
        for br in $branches; do
          echo "\n--------------------------------------------"
          echo "🌿 Scanning branch: $br"
          echo "--------------------------------------------"
          git checkout --force -B "$br" "origin/$br"
          node hof-vulnerabilities-scanner/index.js --out "scan-results-${br}.json" || true
          cnt=$(jq '.vulnerabilitiesCount // 0' "scan-results-${br}.json" 2>/dev/null || echo 0)
          if [ "$cnt" -gt 0 ]; then
            any_compromised=1
            echo "⚠️  Found $cnt compromised packages on $br"
            summary="${summary}${br}: ${cnt} compromised packages\n"
          else
            echo "✅ No compromised packages on $br"
            summary="${summary}${br}: 0\n"
          fi
          echo "🗂️  Results file: scan-results-${br}.json"
        done
        echo "::endgroup::"

        echo "\n============================================"
        echo "📊 Scan Summary"
        echo "============================================"
        printf "%b" "$summary"

        if [ "$any_compromised" -eq 0 ]; then
          final="$repo_name: No compromised packages found. ✅"
        else
          final="$repo_name: Compromised packages found:\n${summary} ❌"
        fi

        echo "\n🧾 Message to Slack:"
        printf "%b\n" "$final"
        echo "summary=$final" >> $GITHUB_ENV
      shell: bash

    - id: prepare-summary
      name: Prepare summary output
      run: |
        # Expose summary as an action output
        printf "summary=%s\n" "${summary}" >> "$GITHUB_OUTPUT"
      shell: bash

    - name: Notify to Slack
      env:
        PREVIEW_MESSAGE: ${{ steps.prepare-summary.outputs.summary }}
      run: |
        echo "\n📣 Preparing to notify Slack..."
        echo "Preview message:"
        printf "%b\n" "$PREVIEW_MESSAGE"
      shell: bash

    - name: Notify Slack
      env:
        SLACK_WEBHOOK_URL: ${{ inputs.slack-webhook-url }}
        SCAN_OUTPUT: ${{ steps.prepare-summary.outputs.summary }}
      run: |
        echo "\n🚀 Sending Slack notification..."
        payload=$(jq -n --arg text "$SCAN_OUTPUT" '{text: $text}')
        echo "Payload:" && echo "$payload"

        # Capture HTTP status separately from body.
        http_code=$(curl -s -o /tmp/slack_body.txt -w "%{http_code}" -X POST -H 'Content-type: application/json' --data "$payload" "$SLACK_WEBHOOK_URL" || echo "000")
        body=$(cat /tmp/slack_body.txt 2>/dev/null || echo "")

        if [ "$http_code" = "200" ]; then
          if [ "$body" = "ok" ]; then
            echo "✅ Slack notified successfully (200)"
          else
            echo "⚠️ Slack responded 200 with unexpected body: $body"
          fi
        else
          echo "❌ Slack notification failed (HTTP $http_code)"
          echo "Response body: $body"
        fi
      shell: bash
